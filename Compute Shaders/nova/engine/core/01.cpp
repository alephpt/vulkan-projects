#include "core.h"

// This becomes our wrapper implementation for the engine level of abstraction in nova. 
// This becomeds our primary interface for the engine, and is the only class that should be exposed to the user.
// This is our hook into the engine.